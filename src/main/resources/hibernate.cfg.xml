<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration SYSTEM
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQLDialect
            <!--org.hibernate.dialect.MySQL5InnoDBDialect-->
        </property>
        <property name="hibernate.connection.driver_class">
            com.mysql.jdbc.Driver
        </property>
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost/tomek
        </property>
        <property name="hibernate.connection.username">
            tomek
        </property>
        <property name="hibernate.connection.password">
            tomek
        </property>

        <!-- Print SQL to stdout. -->
        <!--property name="show_sql">true</property>
        <property name="format_sql">true</property-->

        <!-- disabled for UT speed and for web application load time -->
        <!--property name="hibernate.c3p0.min_size">5</property>
        <property name="hibernate.c3p0.max_size">40</property>
        <property name="hibernate.c3p0.timeout">300</property>
        <property name="hibernate.c3p0.max_statements">50</property>
        <property name="hibernate.c3p0.idle_test_period">3000</property-->

        <!-- to eliminate the error when starting: Disabling contextual LOB creation as createClob() method threw error, and to disable actual checking if connection supports it. not needed. Also too make start time normal - we do not neeed hibernate to fetch metadata -->
        <property name="hibernate.temp.use_jdbc_metadata_defaults">false</property>

    </session-factory>
</hibernate-configuration>